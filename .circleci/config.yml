version: 2.1

jobs:
  test:
    docker:
      - image: cimg/go:1.23
    steps:
      - checkout
      - run:
          name: Ejecutar test con docker
          command: docker run -u 1001 -t -v `pwd`:/app/test imagen 

workflows:
  version: 2
  test-workflow:
    jobs:
      - test
