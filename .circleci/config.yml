version: 2.1

jobs:
  test:
    docker:
      - image: cimg/go:1.17
      - image: cimg/go:1.18
      - image: cimg/go:1.19
      - image: cimg/go:1.20
      - image: cimg/go:1.21
      - image: cimg/go:1.22
      - image: cimg/go:1.23
    steps:
      - checkout
      - run:
          name: Instalar Just
          command: |
            mkdir -p /tmp/bin
            curl -fsSL https://just.systems/install.sh | bash -s -- --to /tmp/bin
      - run:
          name: Verificar instalación de Just
          command: /tmp/bin/just --version
      - run:
          name: Ejecutar tests
          command: /tmp/bin/just test

workflows:
  version: 2
  test-workflow:
    jobs:
      - test
