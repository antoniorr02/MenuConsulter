jobs:
  test:
    docker:
      - image: cirrusci/flutter:stable
    working_directory: ~/repo
    steps:
      - checkout
      - run:
          name: Ejecutar Test
          command: docker run -u 1001 -t -v `pwd`:/app/test imagen